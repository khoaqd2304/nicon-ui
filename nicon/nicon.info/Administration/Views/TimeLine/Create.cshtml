@using Nop.Admin.Models.Timeline
@model TimelineModel

@{
    //page title
    ViewBag.Title = T("Admin.ContentManagement.TimeLine.AddNew").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("TimeLine");
}

@using (Html.BeginForm("Create", "TimeLine", FormMethod.Post, new { enctype = "multipart/form-data" , id = "form-timeline" } ))
{
    @Html.AntiForgeryToken()
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.Common.Addnew")
            <small><i class="fa fa-arrow-circle-left"></i>@Html.ActionLink(T("Admin.ContentManagement.Works.BackToList").Text, "Index")</small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>


















        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)
}


